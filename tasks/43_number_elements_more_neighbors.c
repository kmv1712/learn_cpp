/* Программа вернет количесто элементов которые больше своих соседей */
#include <stdio.h>

int main()
{
    int n;
    scanf( "%d", &n );
    int A[n], count_el = 0;

    for ( int i = 0; i < n; ++i )
        scanf( "%d", &A[i] );

    for ( int i = 0; i < n; ++i )
    {
        if i == 0
        {

        }
    }

    return 0;
}
